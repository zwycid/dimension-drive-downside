package com.hanback.testdb;

import android.content.Context;
import android.graphics.Canvas;
import android.text.TextPaint;
import android.util.AttributeSet;
import android.util.Log;
import android.view.SurfaceHolder;
import android.view.SurfaceView;

public class TotalScoreSurfaceView extends SurfaceView implements SurfaceHolder.Callback, Runnable{

	private Thread thread;
	
	public TotalScoreSurfaceView(Context context, AttributeSet attrs, int defStyle) {
		super(context, attrs, defStyle);
		initView();
	}

	public TotalScoreSurfaceView(Context context, AttributeSet attrs) {
		super(context, attrs);
		initView();
	}

	public TotalScoreSurfaceView(Context context) {
		super(context);
		initView();
	}
	private void initView(){
		this.getHolder().addCallback(this);
	}

	public void surfaceChanged(SurfaceHolder surfaceHolder, int arg1, int arg2, int arg3) {
		
	}

	public void surfaceCreated(SurfaceHolder surfaceHolder) {
		thread	= new Thread(this);
		thread.start();
	}

	public void surfaceDestroyed(SurfaceHolder surfaceHolder) {
		
	}

	public void run() {
		SurfaceHolder holder	= this.getHolder();
		Canvas canvas	= null;
		TextPaint textPaint	= new TextPaint();
		textPaint	= doTextPaint(textPaint);
		
		while(thread != null){
			canvas	= holder.lockCanvas();
			canvas.drawText("test", 100 , 100, textPaint);
			holder.unlockCanvasAndPost(canvas);
		}
		
	}
	
	private TextPaint doTextPaint(TextPaint tPaint){
		
		tPaint.setTextSize(20);
		tPaint.setColor(0xff66ffff);
		tPaint.setShadowLayer(10.0f, 0.0f, 0.0f, 0xff66ffff);
		
		return tPaint;
	}
	
}